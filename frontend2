export interface Action {
  id: string;
  label: string;
  enabled: boolean;
}

export interface Item {
  id: number;
  name: string;
  status: string;
}

export interface DashboardResponse {
  screen: string;
  title: string;
  actions: Action[];
  items: Item[];
}
