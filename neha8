const API_URL = "http://localhost:5000/api";

export async function fetchDashboard() {
  const response = await fetch(`${API_URL}/feature/dashboard`, {
    headers: {
      "X-User-ID": "admin"
    }
  });

  if (!response.ok) {
    throw new Error("Server error");
  }

  return response.json();
}
